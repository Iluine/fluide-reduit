# W2 — Dam-break n-width intra-échantillon (solveur validé W1)

**Encoder forcing-test** : la base POD linéaire représente-t-elle le front mouillé/sec du SOLVEUR (oracle Ritter, W1 validé) ? Mesure INTRA-SAMPLE : un run dam-break BALAYE le domaine → le front est à toutes les positions dans un seul run (pas de confound de couverture).

## Setup

- Grille : 64×64, dx=dy=0.06250 m, domaine [0,4]²
- Dam-break lit sec : hl=0.1, dam à x0=0.5 m, t_end=1.3 s
- Front Ritter à t_end : x_B ≈ 3.0752 m (marge avant mur : 0.9248 m)
- Simulation : 76 frames
- POD : energy_threshold=0.9999, max_modes=2000

## Résultats

| k (modes) | L2 globale | L2 FRONT (gate) |
|---|---|---|
| **7** | 0.0027 | **0.0101** |

### k-sweep (décroissance de l'erreur front)

| k_try (modes tronqués) | front_l2 |
|---|---|
| 5  | 0.0380 |
| 10 (= base complète, k=7) | 0.0101 |
| 20 (= base complète, k=7) | 0.0101 |
| 40 (= base complète, k=7) | 0.0101 |

### Référence W0

- W0 Thacker planaire_diag (front analytique LISSE, transport) : ~7.1%
- Le front Ritter est un touchdown LISSE (h→0 paraboliquement, 1er ordre diffuse davantage) → c'est le barreau FACILE pour la POD.

## Framing

- **Oracle** : Ritter dry-bed (position de front validée W1 ±3 cellules).
- **1er ordre diffuse le front** : chaque interface numérique étale le front → front plus large → plus facile pour la POD linéaire (barreau facile).
- **front_l2 > 20%** → la base linéaire NE TIENT PAS même le front diffusé (décisif, intra-sample, oracle validé, pas de confound) → ENCODEUR JUSTIFIÉ.
- **front_l2 ≤ 20%** → HOLD provisoire : le front diffusé passe, le front net (bore MUSCL 2e ordre) reste à tester.

## Verdict

**HOLD-provisoire** : HOLD (provisoire) : la base tient le front 1er-ordre DIFFUSÉ (barreau facile). front_l2=0.0101 ≤ 0.20. Le front genuinement net (bore) requiert le solveur 2e ordre MUSCL — re-test gaté, prochain pas. PAS les îles. (vs W0 smooth transport 0.071)

Figure : `outputs/v2.5/w2_dambreak.png`
