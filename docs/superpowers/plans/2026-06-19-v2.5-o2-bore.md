# v2.5 — 2e ordre MUSCL + n-width du front net (bore Stoker) — Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans. Steps en cases `- [ ]`.

**Goal:** construire l'instrument 2e ordre (MUSCL well-balanced positivity-preserving sur le solveur mouillé/sec validé), le re-valider analytiquement, puis poser le test décisif : la n-width d'un **front net (bore Stoker)** — qui tranche l'encodeur.

**Architecture:** extension 2e ordre (reconstruction de la **surface libre η=h+b**, surface gradient method / Zhou et al., pente minmod limitée pour la positivité au sec) du solveur 1er-ordre **validé** — chemin SÉPARÉ (le 1er ordre, oracle W1, reste intact → non-régression). Re-validation : 4 analytiques au 2e ordre, **Thacker-2e ordre en gate-conjonction** (WB+sec+mouvement ensemble) + **Stoker** (bore, le régime choc). Test décisif : n-width intra-échantillon front-localisée sur le bore net (apples-to-apples avec W0/W2).

**Tech Stack:** Python 3.12 (`.venv` via `uv`), numpy/scipy/matplotlib. `.venv/bin/python`, `pytest`.

## Global Constraints

- **Frugal CPU ; numpy/scipy/matplotlib seulement.** Pas de `pytorch`/encodeur (gaté sur CE test).
- **Ne PAS casser le 1er ordre validé** : le 2e ordre est un **chemin séparé** dans `src/solver_wetdry.py` (nouvelle fonction `simulate_wetdry_o2`), le 1er ordre `simulate_wetdry` **inchangé**. `src/solver.py` (POC) jamais touché. Non-régression : suite (69 tests) reste verte.
- **Recette standard, recoupée à la littérature** (jamais de mémoire sur le schéma/l'oracle) :
  - 2e ordre WB = **surface gradient method** : reconstruire **η=h+b** (PAS h) → lac au repos (η const) a pente nulle → WB préservé. Réf : Zhou et al. 2001 ; FullSWOF/Audusse-2e.
  - **η/positivité se réconcilient** (flag #1) : reconstruire η sur pente à travers une interface mouillé/sec peut mettre la surface sous le lit → h*<0. Machinerie : limiter la pente ET retomber au 1er ordre (pente 0) dès qu'une face donne h reconstruit < 0. Expliciter, pas « limiter pour h≥0 » seul.
  - Limiteur **minmod** (TVD le plus diffusif mais le plus sûr au sec ; un bore minmod ~3 cellules, plus net que le 1er ordre).
  - **Stoker** (bore lit mouillé) n'est PAS en forme close (flag #2) : état intermédiaire h_m par **root-find** (bisection) sur l'équation transcendante de Rankine-Hugoniot ; validé par résidus R-H ~0.
- **Indexation `array[y,x]`** ; `GRAVITY` depuis `config` ; domaine `[0,4]²`, grille 64×64, `dx=dy=4/64`.
- **Gate d'acceptation de l'oracle 2e ordre** (la masse ne suffit pas) : C-property-2e à 1e-15, **Thacker-2e en conjonction** (L2↓ ET positivité ET courant parasite ~0, simultanément — seul cas WB+sec+mouvement), Stoker (résidus R-H + structure raréfaction/choc), positivité.

---

## Périmètre & ordre

Le module Stoker (Task 1) est une **racine partagée** (validation Task 3 + test Task 4 l'utilisent). Le 2e ordre (Task 2) est l'instrument. Séquence (working tree/index git partagés → séquentiel) : **Task 1 → Task 2 → Task 3 → Task 4**. W3 (dynamique + projection positivity-preserving) et les îles (Carrier-Greenspan) restent hors-scope, gatés sur le verdict de CE test.

## Structure de fichiers

| Fichier | Rôle |
|---|---|
| `src/stoker.py` (créer) | Solution exacte Stoker (bore lit mouillé) : transcendant h_m (bisection) + champs raréfaction/choc. **Oracle choc** (Task 3 + Task 4). |
| `src/solver_wetdry.py` (modifier) | AJOUTER `simulate_wetdry_o2` (2e ordre MUSCL, surface-gradient η, positivité réconciliée). 1er ordre `simulate_wetdry` **inchangé**. |
| `scripts/run_o2_validate.py` (créer) | Re-validation 2e ordre : 4 analytiques + **Thacker-2e conjonction** + Stoker. Porte d'acceptation. |
| `scripts/run_o2_bore_nwidth.py` (créer) | Test décisif : n-width intra-échantillon front-localisée sur le bore net 2e ordre → verdict encodeur (caveat minmod sur « tient »). |
| `tests/test_stoker.py`, `tests/test_solver_wetdry_o2.py`, `tests/test_run_o2.py` (créer) | Tests TDD. |

---

### Task 1 : `src/stoker.py` — bore Stoker exact (oracle choc, auto-validé par R-H)

**Files:** Create `src/stoker.py` ; Test `tests/test_stoker.py`

**Interfaces:**
- Consumes : `config.GRAVITY`.
- Produces :
  - `stoker_star_state(hl, hr) -> tuple[float, float, float]` → `(h_m, u_m, s)` : profondeur/vitesse intermédiaires + vitesse du choc, par root-find sur la transcendante R-H.
  - `stoker_dam_break(x, t, hl=1.0, hr=0.1, x0=2.0) -> tuple[np.ndarray, np.ndarray]` → `(h, u)` sur `x` (vecteur), à `t>0` : 5 zones (h_l ; raréfaction ; état intermédiaire h_m ; choc ; h_r).

**Le schéma (vérifié contre la structure standard SWE Riemann — Stoker 1957 / Toro) :**
- Raréfaction gauche (invariant de Riemann `u + 2√(gh)` conservé depuis `(h_l, 0)`) : `u_m + 2√(g h_m) = 2√(g h_l)` ⇒ `u_m = 2(√(g h_l) − √(g h_m))`.
- Choc droit (Rankine-Hugoniot, bore entrant dans `(h_r, 0)`) : `u_m = (h_m − h_r)·√( (g/2)·(h_m + h_r)/(h_m·h_r) )`.
- **Transcendante en h_m** (égaler les deux `u_m`) : `2(√(g h_l) − √(g h_m)) = (h_m − h_r)√((g/2)(h_m+h_r)/(h_m h_r))`. Résoudre par **bisection** sur `h_m ∈ (h_r, h_l)` (membre gauche décroît, droit croît en h_m → racine unique).
- Vitesse du choc : `s = h_m u_m / (h_m − h_r)`.

- [ ] **Step 1 : Tests qui échouent — AUTO-VALIDATION par les sauts (pas de valeur externe requise)**

```python
# tests/test_stoker.py
import numpy as np
import pytest
from config import GRAVITY
from src.stoker import stoker_star_state, stoker_dam_break


def test_star_state_satisfies_rankine_hugoniot_and_riemann():
    # Le coeur de la validation flag #2 : un coefficient faux VIOLE ces résidus.
    g = GRAVITY
    hl, hr = 1.0, 0.1
    h_m, u_m, s = stoker_star_state(hl, hr)
    assert hr < h_m < hl                                   # état intermédiaire encadré
    # Invariant de Riemann sur la raréfaction (gauche) :
    assert u_m + 2 * np.sqrt(g * h_m) == pytest.approx(2 * np.sqrt(g * hl), rel=1e-8)
    # Rankine-Hugoniot à travers le choc (état (h_m,u_m) vs (hr,0)), résidus ~0 :
    #   masse : s(h_m - hr) = h_m u_m - hr*0
    #   qdm   : s(h_m u_m - 0) = (h_m u_m^2 + g h_m^2/2) - (g hr^2/2)
    assert s * (h_m - hr) == pytest.approx(h_m * u_m, rel=1e-8)
    assert s * (h_m * u_m) == pytest.approx(
        h_m * u_m ** 2 + 0.5 * g * h_m ** 2 - 0.5 * g * hr ** 2, rel=1e-8)


def test_dam_break_zones_and_positivity():
    hl, hr, x0, t = 1.0, 0.1, 2.0, 0.2
    x = np.linspace(0, 4, 2001)
    h, u = stoker_dam_break(x, t, hl, hr, x0)
    assert (h > 0).all()                                   # lit mouillé partout (h_r>0)
    assert h[x < x0 - t * np.sqrt(GRAVITY * hl)] == pytest.approx(hl)  # plateau gauche intact
    h_m, u_m, s = stoker_star_state(hl, hr)
    # loin devant le choc (x > x0 + s t) : état non perturbé h_r
    assert h[x > x0 + s * t + 0.1] == pytest.approx(hr)
    # juste derrière le choc : l'état intermédiaire h_m (un plateau)
    mid = (x > x0 + 0.05) & (x < x0 + s * t - 0.05)
    assert np.median(h[mid]) == pytest.approx(h_m, rel=5e-2)


def test_dam_break_mass_monotone_front():
    # le choc (front net) se déplace : à deux instants, sa position avance de s*dt
    hl, hr, x0 = 1.0, 0.1, 2.0
    h_m, u_m, s = stoker_star_state(hl, hr)
    h1, _ = stoker_dam_break(np.linspace(0, 4, 4001), 0.10, hl, hr, x0)
    h2, _ = stoker_dam_break(np.linspace(0, 4, 4001), 0.20, hl, hr, x0)
    xs = np.linspace(0, 4, 4001)
    front1 = xs[h1 > 0.5 * (h_m + hr)].max()
    front2 = xs[h2 > 0.5 * (h_m + hr)].max()
    assert front2 - front1 == pytest.approx(s * 0.10, abs=2 * (4 / 4001))  # avance = s·dt
```

- [ ] **Step 2 : Lancer → FAIL** (`ModuleNotFoundError: src.stoker`).

- [ ] **Step 3 : Implémenter `src/stoker.py`**

```python
"""Solution exacte de Stoker (1957) : rupture de barrage sur lit MOUILLÉ (bore).
Structure : raréfaction gauche + état intermédiaire (h_m,u_m) + CHOC droit dans (h_r,0).
h_m n'est PAS en forme close -> bisection sur la transcendante de Rankine-Hugoniot.
Vérifié par les résidus R-H (cf. tests). Oracle du FRONT NET (vraie discontinuité)."""
from __future__ import annotations

import numpy as np

from config import GRAVITY


def stoker_star_state(hl: float, hr: float) -> tuple[float, float, float]:
    """(h_m, u_m, s) par bisection : raréfaction (u_m=2(√(ghl)−√(ghm))) = choc R-H."""
    g = GRAVITY
    def f(hm):  # raréfaction − choc, racine en h_m ∈ (hr, hl)
        u_rar = 2.0 * (np.sqrt(g * hl) - np.sqrt(g * hm))
        u_shk = (hm - hr) * np.sqrt(0.5 * g * (hm + hr) / (hm * hr))
        return u_rar - u_shk
    lo, hi = hr * (1.0 + 1e-9), hl * (1.0 - 1e-9)
    for _ in range(200):
        mid = 0.5 * (lo + hi)
        if f(lo) * f(mid) <= 0.0:
            hi = mid
        else:
            lo = mid
    h_m = 0.5 * (lo + hi)
    u_m = 2.0 * (np.sqrt(g * hl) - np.sqrt(g * h_m))
    s = h_m * u_m / (h_m - hr)
    return float(h_m), float(u_m), float(s)


def stoker_dam_break(x, t: float, hl: float = 1.0, hr: float = 0.1,
                     x0: float = 2.0) -> tuple[np.ndarray, np.ndarray]:
    """h(x,t), u(x,t) pour t>0 : 5 zones (h_l | raréfaction | h_m | choc | h_r)."""
    x = np.asarray(x, dtype=np.float64)
    g = GRAVITY
    if t <= 0:
        return np.where(x <= x0, hl, hr), np.zeros_like(x)
    h_m, u_m, s = stoker_star_state(hl, hr)
    cl = np.sqrt(g * hl)
    xi = (x - x0) / t                                  # variable d'auto-similarité
    x_raref_head = -cl                                 # tête de raréfaction (vers la gauche)
    x_raref_tail = u_m - np.sqrt(g * h_m)              # queue de raréfaction
    h = np.empty_like(x); u = np.empty_like(x)
    # zone 1 : plateau gauche
    m1 = xi <= x_raref_head
    h[m1] = hl; u[m1] = 0.0
    # zone 2 : raréfaction (éventail) — h=(2cl−xi)^2/(9g), u=2(cl+xi)/3
    m2 = (xi > x_raref_head) & (xi < x_raref_tail)
    h[m2] = (2.0 * cl - xi[m2]) ** 2 / (9.0 * g)
    u[m2] = 2.0 * (cl + xi[m2]) / 3.0
    # zone 3 : état intermédiaire
    m3 = (xi >= x_raref_tail) & (xi < s)
    h[m3] = h_m; u[m3] = u_m
    # zone 4 : aval du choc (non perturbé)
    m4 = xi >= s
    h[m4] = hr; u[m4] = 0.0
    return h, u
```

- [ ] **Step 4 : Lancer → PASS** (3 tests : R-H + Riemann auto-validés, zones, front mobile).
- [ ] **Step 5 : Commit** `git add src/stoker.py tests/test_stoker.py && git commit -m "feat(v2.5): Stoker exact (bore lit mouillé) — root-find R-H auto-validé, oracle front net"`

---

### Task 2 : `src/solver_wetdry.py` — 2e ordre MUSCL (surface-gradient η + positivité réconciliée)

**Files:** Modify `src/solver_wetdry.py` (ajout) ; Test `tests/test_solver_wetdry_o2.py`

**Interfaces:**
- Consumes : tout l'existant de `solver_wetdry.py` (HLL `_hll_flux`, reconstruction hydrostatique, désingularisation, SSP-RK2) ; `config`.
- Produces : `simulate_wetdry_o2(h0, hu0, hv0, b, grid, cfl=0.4, t_end=2.0, dry_eps=1e-4) -> (times, h_seq, hu_seq, hv_seq)` — **2e ordre**, même signature que `simulate_wetdry`.

**Recette 2e ordre (recouper contre Zhou et al. / FullSWOF-2e ; flag #1 explicite) :**
- Calculer `η = h + b` par cellule. Reconstruire **η** (pas h), `u`, `v` linéairement : pente par cellule = **minmod**(différences avant/arrière). Valeurs aux faces : `η_L = η_i − ½·slopeη_i`, `η_R = η_i + ½·slopeη_i` (idem u,v).
- **Réconciliation η/positivité (le crux)** : aux faces, `h_face = η_face − b_face`. Si une face donne `h_face < 0` (la pente η a mis la surface sous le lit), **annuler la pente de cette cellule** (retomber au 1er ordre : η_L=η_R=η_i) — détecter/limiter/retomber, pas seulement clamp. Reconstruire `b` aux faces de façon cohérente (b par cellule, ou b aux faces partagé).
- Puis : reconstruction **hydrostatique** d'Audusse sur les états reconstruits (h*_L/R via η reconstruits et z*=max), **HLL** (réutiliser `_hll_flux`), correction de pression — exactement comme le 1er ordre, mais sur les états MUSCL.
- **SSP-RK2** (déjà là) + plancher sec après chaque étage + CFL positivity-preserving.

- [ ] **Step 1 : Tests qui échouent (invariants 2e ordre)**

```python
# tests/test_solver_wetdry_o2.py
import numpy as np
import pytest
from config import GridConfig, GRAVITY
from src.solver_wetdry import simulate_wetdry, simulate_wetdry_o2

GRID = GridConfig(H=48, W=48, dx=4.0 / 48, dy=4.0 / 48)


def test_o2_c_property_lake_at_rest_emergent():
    # WB au 2e ordre : reconstruire η (pas h) DOIT garder le lac au repos -> vitesse ~0.
    # (Un schéma qui reconstruit h casserait ici.)
    yy, xx = np.mgrid[0:48, 0:48].astype(float)
    b = 0.6 * np.exp(-(((xx - 24) ** 2 + (yy - 24) ** 2) / 30.0))
    eta0 = 0.4
    h0 = np.maximum(eta0 - b, 0.0); z = np.zeros_like(h0)
    _, hs, hus, hvs = simulate_wetdry_o2(h0, z.copy(), z.copy(), b, GRID, t_end=2.0)
    speed = np.sqrt((hus / np.maximum(hs, 1e-6)) ** 2 + (hvs / np.maximum(hs, 1e-6)) ** 2)
    assert float(speed.max()) < 1e-6                       # WB 2e ordre tenu
    assert float(hs.min()) >= 0.0                          # positivité


def test_o2_positivity_on_drying():
    yy, xx = np.mgrid[0:48, 0:48].astype(float)
    h0 = np.maximum(0.5 * np.exp(-(((xx - 24) ** 2 + (yy - 24) ** 2) / 20.0)) - 0.05, 0.0)
    z = np.zeros_like(h0)
    _, hs, _, _ = simulate_wetdry_o2(h0, z.copy(), z.copy(), np.zeros_like(h0), GRID, t_end=1.0)
    assert float(hs.min()) >= 0.0 and np.isfinite(hs).all()  # réconciliation η/positivité OK


def test_o2_sharper_than_o1_on_dam_break():
    # le 2e ordre résout un front plus NET que le 1er ordre (moins de cellules de transition)
    xs = (np.arange(48) + 0.5) * GRID.dx
    h0 = np.where(xs[None, :] <= 2.0, 1.0, 0.1) * np.ones((48, 48))
    z = np.zeros((48, 48))
    def front_width(sim):
        _, hs, _, _ = sim(h0.copy(), z.copy(), z.copy(), z.copy(), GRID, t_end=0.3)
        row = hs[-1, 24, :]                                # profil final
        # largeur de la zone de transition du choc (entre 0.15 et h proche de h_m)
        trans = np.sum((row > 0.15) & (row < 0.9))
        return trans
    assert front_width(simulate_wetdry_o2) <= front_width(simulate_wetdry)  # plus net (ou égal)
```

- [ ] **Step 2 : Lancer → FAIL** (`ImportError: simulate_wetdry_o2`).
- [ ] **Step 3 : Implémenter `simulate_wetdry_o2`** (recette ci-dessus, en réutilisant le noyau flux/recon/RK2 du 1er ordre ; ne PAS modifier `simulate_wetdry`). **Faire passer `test_o2_c_property_lake_at_rest_emergent` D'ABORD** — c'est le test du η-reconstruction ; s'il échoue, on reconstruit h au lieu de η, ou la réconciliation est fausse → recouper Zhou et al./FullSWOF-2e.
- [ ] **Step 4 : Lancer → PASS** (3 invariants).
- [ ] **Step 5 : Non-régression 1er ordre** : `.venv/bin/python -m pytest -q` → 69 + nouveaux passent ; `simulate_wetdry` (1er ordre) inchangé.
- [ ] **Step 6 : Commit** `git add src/solver_wetdry.py tests/test_solver_wetdry_o2.py && git commit -m "feat(v2.5): solveur 2e ordre MUSCL (surface-gradient η, positivité réconciliée), invariants"`

---

### Task 3 : `scripts/run_o2_validate.py` — re-validation 2e ordre (Thacker-2e en gate-conjonction + Stoker)

**Files:** Create `scripts/run_o2_validate.py` ; `docs/v2.5_o2_validation.md` (écrit par le script)

**Interfaces:**
- Consumes : `simulate_wetdry_o2` ; `src.analytic_thacker` (`thacker_radial`, période) ; `src.ritter.ritter_dam_break_dry` ; `src.stoker` (`stoker_dam_break`, `stoker_star_state`) ; `src.metrics.relative_l2_error` ; `config`.
- Produces : `main()` exécutant les validations 2e ordre, chaque critère un `assert`.

**Validations (la porte d'acceptation de l'oracle 2e ordre) :**
1. **Thacker-2e — GATE-CONJONCTION (flag #1, le test du η/positivité ensemble)** : init `thacker_radial(t=0)`, simuler ~1 période en 2e ordre. Asserter **simultanément** : (a) max L2 < 0.30 **ET plus bas que le 1er ordre** (le 2e ordre est plus précis sur le trait d'eau mobile) ; (b) `min(h) ≥ 0` sur toutes les frames (positivité tenue sur pente+sec+mouvement) ; (c) courant parasite résiduel borné. C'est LA validation qui exerce WB+sec+mouvement ensemble — la conjonction, pas trois checks séparés.
2. **Stoker (bore, front net — régime choc)** : init dam-break lit mouillé (hl=1, hr=0.1, x0=2), simuler en 2e ordre à t court, comparer à `stoker_dam_break(t)` : position du choc à ±(2-3 cellules) de `x0 + s·t`, état intermédiaire h_m correct (médiane derrière le choc), `min(h)≥0`. **Le choc doit être plus net qu'au 1er ordre** (largeur de transition rapportée).
3. **C-property-2e** (île émergente statique) : vitesse parasite max < 1e-6.
4. **Positivité** : `min(h) ≥ 0` partout, toutes validations.

- [ ] **Step 1 : Implémenter `scripts/run_o2_validate.py`** (assemble les 4 ; **Thacker-2e en conjonction** ; chaque critère un `assert` ; figures vérité|2e-ordre|résidu pour Thacker et Stoker ; doc `docs/v2.5_o2_validation.md`).
- [ ] **Step 2 : Lancer** `.venv/bin/python scripts/run_o2_validate.py`. Porte : les 4 passent (surtout la **conjonction Thacker-2e**) → l'oracle 2e ordre est accepté pour le régime net. Un échec de la conjonction (positivité perd OU L2 ne baisse pas OU courant parasite) = la réconciliation η/positivité est fausse → recouper, **ne pas desserrer**.
- [ ] **Step 3 : Non-régression** `.venv/bin/python -m pytest -q`.
- [ ] **Step 4 : Commit** `git add scripts/run_o2_validate.py docs/v2.5_o2_validation.md && git commit -m "feat(v2.5): validation 2e ordre (Thacker-2e conjonction WB/sec/mouvement + Stoker bore)"`

---

### Task 4 : `scripts/run_o2_bore_nwidth.py` — le test DÉCISIF (n-width du bore net)

**Files:** Create `scripts/run_o2_bore_nwidth.py` ; `docs/v2.5_o2_bore_nwidth.md` (écrit par le script)

**Interfaces:**
- Consumes : `simulate_wetdry_o2` ; `src.stoker` ; `src.analytic_thacker.front_band_mask` ; `scripts.run_w0_representation.band_l2_error` ; `src.pod` (`fit_pod n_channels=1`, encode/decode, stack/unstack_height) ; `src.metrics.relative_l2_error` ; `config`.
- Produces : `main()` — n-width intra-échantillon front-localisée du bore 2e ordre + verdict.

**Le test (apples-to-apples avec W0/W2, mais sur un FRONT NET) :**
- Dam-break lit mouillé (Stoker, hl=1, hr=0.1, x0=0.5 pour un long balayage), 2e ordre, `t_end` tel que le bore balaie vers ~3.3 (intérieur). Le bore (front net) balaie → snapshots à toutes ses positions.
- POD hauteur intra-échantillon (`n_channels=1`, 0.9999) sur le balayage ; **plafond front-localisé** (`band_l2_error`) vs k ; balayage k=[5,10,20,40] (annoter k_eff si capé) pour la décroissance n-width.
- Comparer à **W0 (transport lisse 7.1 %) et W2 (front diffusé 1er-ordre 1.0 %)** — l'escalade de netteté : lisse → diffusé → **net**.

- [ ] **Step 1 : Implémenter `scripts/run_o2_bore_nwidth.py`** (réutilise band_l2_error/front_band_mask/pod ; figure ; doc).
- [ ] **Step 2 : Lancer** `.venv/bin/python scripts/run_o2_bore_nwidth.py`. Rapporter k, global L2, **FRONT L2**, k-sweep, vs W0/W2.
- [ ] **Step 3 : Verdict (câblé, avec le caveat minmod — flag #3) :**
  - `front L2 > 0.20` → **DÉCISIF : encodeur justifié** — la base linéaire ne représente pas un choc mobile (la n-width mord enfin, sur oracle validé, intra-échantillon, sans confond de couverture). Le test forçant attendu depuis le POC.
  - `front L2 ≤ 0.20` → **TIENT, mais scopé à la netteté de minmod** : « tient sur un bore minmod (~3 cellules) » ≠ « tient sur une vraie discontinuité ». Si le livrable visuel exige des fronts plus raides → MC/van Leer (plus net, TVD) est le barreau suivant. **« Casse » serait décisif quel que soit le limiteur ; « tient » est provisoire au limiteur.**
- [ ] **Step 4 : Non-régression** + Commit `git add scripts/run_o2_bore_nwidth.py docs/v2.5_o2_bore_nwidth.md && git commit -m "feat(v2.5): test décisif — n-width du bore net (2e ordre), verdict encodeur"`

---

## Self-Review

- **Couverture** : instrument 2e ordre (Task 2, surface-gradient η + positivité réconciliée) ✓ ; oracle choc Stoker (Task 1, R-H auto-validé) ✓ ; re-validation avec **Thacker-2e en conjonction** (flag #1, Task 3) ✓ ; test décisif n-width du bore net intra-échantillon (Task 4) ✓ ; caveat minmod sur « tient » (flag #3, Task 4) ✓ ; Stoker root-find + R-H (flag #2, Task 1) ✓ ; 1er ordre intact / non-régression (Tasks 2,3,4) ✓.
- **Placeholders** : Stoker en code complet (oracle, auto-validé R-H). Le corps `simulate_wetdry_o2` (Task 2 step 3) est en **recette explicite + invariants-gate** (comme le 1er ordre W1) — délibéré : la correction de la réconciliation η/positivité est garantie par la **conjonction Thacker-2e**, pas par 200 lignes potentiellement fausses ; la partie piégeuse (réconciliation) est signalée à recouper (Zhou/FullSWOF).
- **Cohérence types** : `simulate_wetdry_o2(h0,hu0,hv0,b,grid,...) -> (times,h_seq,hu_seq,hv_seq)` (mime le 1er ordre) ; `stoker_star_state -> (h_m,u_m,s)`, `stoker_dam_break -> (h,u)` ; `band_l2_error`/`front_band_mask`/`fit_pod n_channels=1` réutilisés (W0/W2).

---

## Execution Handoff

Plan sauvegardé. Exécution : **subagent-driven-development**, séquentiel (Task 1 racine Stoker → Task 2 solveur 2e ordre → Task 3 validation-conjonction → Task 4 test décisif). Le verdict de Task 4 gate l'encodeur. W3 (projection positivity-preserving) et les îles (Carrier-Greenspan) restent hors-scope.
