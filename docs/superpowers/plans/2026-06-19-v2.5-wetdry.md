# v2.5 — Régime mouillé/sec : W0 (test forçant analytique) ∥ W1 (solveur) — Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** (W0) mesurer si une base POD linéaire représente un **trait d'eau mobile** (test n-width, oracle analytique Thacker) ; (W1) construire un **solveur shallow-water mouillé/sec** (well-balanced + positivity-preserving) validé contre solutions analytiques, qui servira d'oracle à W2.

**Architecture:** W0 et W1 sont **indépendants et parallèles** (W0 n'a pas besoin du solveur — il POD-reconstruit des champs analytiques en forme close). W1 est un **nouveau** schéma (reconstruction hydrostatique d'Audusse 2004 + flux HLL + gestion mouillé/sec + SSP-RK2, 1er ordre), **copié** à part de `src/solver.py` (jamais modifié → non-régression du régime submergé, tag `v1`). Sa correction est garantie non par la masse mais par **4 validations analytiques** (Thacker, Ritter, C-property, positivité).

**Tech Stack:** Python 3.12 (`.venv` via `uv`), numpy/scipy/matplotlib uniquement. Lancement `.venv/bin/python …`, tests `.venv/bin/python -m pytest`.

## Global Constraints

- **Frugal, CPU, numpy/scipy/matplotlib seulement.** Pas de `pytorch`/encodeur (gaté W0/W2). Demander avant toute dépendance.
- **Ne PAS modifier `src/solver.py`** (Rusanov submergé, tag `v1`) : le solveur mouillé/sec est un **nouveau** fichier `src/solver_wetdry.py`. Non-régression : la suite existante (55 tests) doit rester verte.
- **Réutiliser** `src/pod.py` (`fit_pod`/`encode`/`decode`/`stack_height`/`unstack_height`, `n_channels=1`), `src/metrics.py` (`relative_l2_error`, `error_growth`), `src/io_utils.py` (`save_animation`), `config.GridConfig`.
- **L'oracle ne vaut que ses validations** : W1 n'est pas digne de confiance tant que les **4** validations analytiques ne passent pas. La conservation de masse est nécessaire mais **pas suffisante**.
- **W0 : gate sur l'erreur FRONT-LOCALISÉE, pas la L2 globale.** Le bulk lisse axisymétrique est bas-rang et diluerait la difficulté n-width du front (faux « survit »). La décision se lit sur la bande au voisinage du trait d'eau mobile.
- **W0 : deux oracles** — Thacker radial (front courbe qui *respire* = stress de *forme*) ET Thacker planaire orienté en diagonale (front qui *translate* = stress de *transport*, le tueur n-width canonique). Les deux sont analytiques (gratuits, sans solveur) ; les faire tourner désambiguïse forme vs transport.
- Indexation `array[y, x]`. `GRAVITY = 9.81` (depuis `config`).
- Domaine v2.5 : `[0,L]²` avec `L=4`, grille 64×64 → `dx = dy = L/64 = 0.0625`.
- Formules analytiques **vérifiées** dans SWASHES (Delestre et al. 2016, arXiv:1110.0288, §4.2.1/§4.2.2 Thacker, §4.1.2 Ritter). Le schéma W1 suit Audusse et al. 2004 (SIAM J. Sci. Comput. 25(6):2050) ; **recouper le terme source de la reconstruction hydrostatique contre cette référence** (partie la plus piégeuse — la C-property l'attrape).

---

## Périmètre & ordre

W0 et W1 sont indépendants → exécutables en parallèle (subagents distincts ne partagent aucun fichier). W2 (plafond sur la vraie dynamique mouillé/sec) et W3 (dynamique + projection positivity-preserving) **ne sont pas détaillés** : W2 sera planifié après que l'oracle W1 a passé ses 4 validations ; W3 après le chiffre de W2 (même discipline que V0+V1, puis V2). Note d'anticipation W3 : la projection de masse M5 (offset uniforme) **ne marche plus** en sec (noierait les cellules sèches) → il faudra une projection masse-conservante **et** positivity-preserving (redistribution sur les seules cellules mouillées).

## Structure de fichiers

| Fichier | Rôle |
|---|---|
| `src/analytic_thacker.py` (créer) | Solutions analytiques exactes Thacker (radial + planaire diagonal) + masque de bande-front. **Oracle de W0, et référence de validation de W1.** |
| `scripts/run_w0_representation.py` (créer) | W0 : POD hauteur sur les snapshots Thacker, plafond vs k, **erreur front-localisée**, verdict + figures. Parallèle. |
| `src/ritter.py` (créer) | Solution analytique exacte de Ritter (dam-break sur lit sec). Référence de validation W1. |
| `src/solver_wetdry.py` (créer) | Solveur mouillé/sec : reconstruction hydrostatique + HLL + gestion sec + SSP-RK2 (1er ordre). **Copié, NE modifie pas `solver.py`.** |
| `scripts/run_w1_validate.py` (créer) | W1 : les 4 validations analytiques (Thacker, Ritter, C-property, positivité) = porte d'acceptation + figures. |
| `tests/test_analytic_thacker.py`, `tests/test_ritter.py`, `tests/test_solver_wetdry.py`, `tests/test_run_w0.py` (créer) | Tests TDD. |

---

### Task 1 : `src/analytic_thacker.py` — Thacker radial (oracle de forme)

**Files:** Create `src/analytic_thacker.py` ; Test `tests/test_analytic_thacker.py`

**Interfaces:**
- Consumes : `config.GRAVITY`, `config.GridConfig`.
- Produces :
  - `thacker_radial(grid, t, a=1.0, h0=0.1, r0=0.8, L=4.0) -> tuple[np.ndarray, np.ndarray]` → `(h, b)` chacun `(H,W)`, `h ≥ 0` (clampé). `b` est la bathymétrie (paraboloïde).
  - `thacker_radial_period(a=1.0, h0=0.1) -> float` → période `T = 2π/ω`, `ω = √(8 g h0)/a`.

- [ ] **Step 1 : Test qui échoue**

```python
# tests/test_analytic_thacker.py
import numpy as np
import pytest
from config import GridConfig, GRAVITY
from src.analytic_thacker import thacker_radial, thacker_radial_period

GRID = GridConfig(H=64, W=64, dx=4.0 / 64, dy=4.0 / 64)


def test_radial_period():
    a, h0 = 1.0, 0.1
    omega = np.sqrt(8 * GRAVITY * h0) / a
    assert thacker_radial_period(a, h0) == pytest.approx(2 * np.pi / omega)


def test_radial_shapes_and_positivity():
    h, b = thacker_radial(GRID, t=0.0)
    assert h.shape == b.shape == (64, 64)
    assert (h >= 0).all()                         # hauteur d'eau toujours positive
    assert b.min() < 0 < b.max() or b.min() < b.max()  # paraboloïde (bol)


def test_radial_has_moving_wet_dry_front():
    # à deux phases différentes, l'aire mouillée (h>seuil) diffère -> shoreline mobile
    T = thacker_radial_period()
    h0_, _ = thacker_radial(GRID, t=0.0)
    h1_, _ = thacker_radial(GRID, t=T / 4)
    wet0 = (h0_ > 1e-3).sum()
    wet1 = (h1_ > 1e-3).sum()
    assert wet0 != wet1                           # le trait d'eau s'est déplacé
    assert wet0 > 0 and wet1 > 0                  # il reste de l'eau


def test_radial_mass_conserved_across_phases():
    # solution exacte sans friction -> masse (intégrale de h) constante dans le temps
    T = thacker_radial_period()
    m = [float(thacker_radial(GRID, t=ph * T)[0].sum()) for ph in (0.0, 0.13, 0.37, 0.5)]
    assert max(m) - min(m) < 0.02 * max(m)        # ~constante (erreur de discrétisation)
```

- [ ] **Step 2 : Lancer → FAIL** (`ModuleNotFoundError: src.analytic_thacker`).

`.venv/bin/python -m pytest tests/test_analytic_thacker.py -q`

- [ ] **Step 3 : Implémenter (verbatim — formules vérifiées SWASHES §4.2.2)**

```python
"""Solutions analytiques exactes de Thacker (shallow-water, trait d'eau MOBILE).

Vérifiées dans SWASHES (Delestre et al. 2016, arXiv:1110.0288, §4.2.1/§4.2.2).
Servent (a) d'oracle de représentation pour W0 (POD d'un front mobile, test n-width),
(b) de référence de validation pour le solveur mouillé/sec W1.

Indexation array[y, x]. h = hauteur d'eau (>=0). b = bathymétrie (z). Surface η = h + b."""
from __future__ import annotations

import numpy as np

from config import GRAVITY, GridConfig


def _centers(grid: GridConfig) -> tuple[np.ndarray, np.ndarray]:
    """Coordonnées des centres de cellules (x, y), chacune (H, W). Domaine [0, L]²."""
    xs = (np.arange(grid.W) + 0.5) * grid.dx
    ys = (np.arange(grid.H) + 0.5) * grid.dy
    xx, yy = np.meshgrid(xs, ys)  # (H, W), indexation [y, x]
    return xx, yy


def thacker_radial_period(a: float = 1.0, h0: float = 0.1) -> float:
    """Période T = 2π/ω, ω = √(8 g h0)/a (paraboloïde radial, sans friction)."""
    omega = np.sqrt(8.0 * GRAVITY * h0) / a
    return 2.0 * np.pi / omega


def thacker_radial(grid: GridConfig, t: float, a: float = 1.0, h0: float = 0.1,
                   r0: float = 0.8, L: float = 4.0) -> tuple[np.ndarray, np.ndarray]:
    """Paraboloïde radial oscillant (SWASHES §4.2.2, sans friction). Shoreline = cercle
    de rayon variable (le front RESPIRE sur place). Retourne (h, b), h clampé >= 0."""
    xx, yy = _centers(grid)
    r2 = (xx - L / 2.0) ** 2 + (yy - L / 2.0) ** 2
    b = -h0 * (1.0 - r2 / a ** 2)                       # z(r), paraboloïde (bol)
    omega = np.sqrt(8.0 * GRAVITY * h0) / a
    A = (a ** 2 - r0 ** 2) / (a ** 2 + r0 ** 2)
    denom = 1.0 - A * np.cos(omega * t)
    eta = h0 * (np.sqrt(1.0 - A ** 2) / denom - 1.0
                - (r2 / a ** 2) * ((1.0 - A ** 2) / denom ** 2 - 1.0))
    h = np.maximum(eta - b, 0.0)
    return h, b
```

- [ ] **Step 4 : Lancer → PASS** (4 tests). `.venv/bin/python -m pytest tests/test_analytic_thacker.py -q`

- [ ] **Step 5 : Commit**

```bash
git add src/analytic_thacker.py tests/test_analytic_thacker.py
git commit -m "feat(v2.5): Thacker radial analytique (oracle front mobile courbe, W0)"
```

---

### Task 2 : `src/analytic_thacker.py` — Thacker planaire diagonal (oracle de transport) + bande-front

**Files:** Modify `src/analytic_thacker.py` ; Test `tests/test_analytic_thacker.py` (append)

**Interfaces:**
- Consumes : Task 1 (`_centers`, `GRAVITY`).
- Produces :
  - `thacker_planar_diag(grid, t, a=1.0, h0=0.5, L=4.0) -> tuple[np.ndarray, np.ndarray]` → `(h, b)`. Front DROIT orienté à 45° qui **translate** le long de la diagonale (SWASHES §4.2.1 dans une coordonnée pivotée).
  - `thacker_planar_period(a=1.0, h0=0.5) -> float` → `T = 2π/(√(2 g h0)/a)`.
  - `front_band_mask(h, eps=1e-3, width=2) -> np.ndarray` (bool, `(H,W)`) → cellules mouillées au voisinage (`width` cellules) du trait d'eau (interface mouillé/sec).

- [ ] **Step 1 : Tests qui échouent (append)**

```python
# tests/test_analytic_thacker.py (append)
from src.analytic_thacker import (thacker_planar_diag, thacker_planar_period,
                                  front_band_mask)


def test_planar_diag_translates_and_is_2d():
    T = thacker_planar_period()
    h0f, _ = thacker_planar_diag(GRID, t=0.0)
    hqf, _ = thacker_planar_diag(GRID, t=T / 4)
    assert h0f.shape == (64, 64) and (h0f >= 0).all()
    # 2D réel : varie selon x ET y (pas extrudé le long d'un axe de grille)
    assert not np.allclose(h0f, h0f[0, :][None, :])   # varie en y
    assert not np.allclose(h0f, h0f[:, 0][:, None])   # varie en x
    # le centroïde mouillé se DÉPLACE entre deux phases (translation du front)
    def centroid(h):
        m = h > 1e-3
        yy, xx = np.mgrid[0:64, 0:64]
        return (xx[m].mean(), yy[m].mean()) if m.any() else (np.nan, np.nan)
    c0, c1 = centroid(h0f), centroid(hqf)
    assert abs(c0[0] - c1[0]) + abs(c0[1] - c1[1]) > 1.0   # translation nette


def test_front_band_mask_hugs_shoreline():
    h, _ = thacker_radial(GRID, t=0.0)
    band = front_band_mask(h, eps=1e-3, width=2)
    assert band.dtype == bool and band.shape == (64, 64)
    assert band.sum() > 0
    assert (h[band] > 0).all()                    # la bande est dans la zone mouillée
    # la bande exclut le coeur profond (on garde le voisinage du trait d'eau)
    assert band.sum() < (h > 1e-3).sum()
```

- [ ] **Step 2 : Lancer → FAIL** (import manquant).

- [ ] **Step 3 : Implémenter (append à `src/analytic_thacker.py`)**

```python
# --- Thacker planaire 1D (SWASHES §4.2.1) orienté en diagonale -> front translatant 2D ---

def thacker_planar_period(a: float = 1.0, h0: float = 0.5) -> float:
    """Période du slosh planaire : T = 2π/ω, ω = √(2 g h0)/a."""
    return 2.0 * np.pi / (np.sqrt(2.0 * GRAVITY * h0) / a)


def thacker_planar_diag(grid: GridConfig, t: float, a: float = 1.0, h0: float = 0.5,
                        L: float = 4.0) -> tuple[np.ndarray, np.ndarray]:
    """Solution planaire 1D de Thacker appliquée le long de la DIAGONALE (45°) : un front
    droit qui TRANSLATE le long de s = ((x-L/2)+(y-L/2))/√2. Solution SWE exacte (rotation
    rigide d'une solution 1D). Bol parabolique en s ; surface planaire oscillante."""
    xx, yy = _centers(grid)
    s = ((xx - L / 2.0) + (yy - L / 2.0)) / np.sqrt(2.0)   # coordonnée diagonale
    b = h0 * (s ** 2 / a ** 2 - 1.0)                        # canal parabolique le long de s
    omega = np.sqrt(2.0 * GRAVITY * h0) / a
    B = np.sqrt(2.0 * GRAVITY * h0) / (2.0 * a)
    inner = (s / a) + (B / np.sqrt(2.0 * GRAVITY * h0)) * np.cos(omega * t)
    h = np.maximum(-h0 * (inner ** 2 - 1.0), 0.0)           # >0 sur la bande mouillée
    return h, b


def front_band_mask(h: np.ndarray, eps: float = 1e-3, width: int = 2) -> np.ndarray:
    """Cellules MOUILLÉES au voisinage (width cellules) du trait d'eau (frontière
    mouillé/sec). C'est là que se concentre l'erreur n-width ; le gate W0 s'y lit."""
    wet = h > eps
    dry = ~wet
    # dilatation binaire de la zone sèche de `width` cellules (voisinage 4-connexe itéré)
    grown = dry.copy()
    for _ in range(width):
        g = grown.copy()
        g[1:, :] |= grown[:-1, :]; g[:-1, :] |= grown[1:, :]
        g[:, 1:] |= grown[:, :-1]; g[:, :-1] |= grown[:, 1:]
        grown = g
    return wet & grown                                       # mouillé ET proche du sec
```

- [ ] **Step 4 : Lancer → PASS.** `.venv/bin/python -m pytest tests/test_analytic_thacker.py -q`

- [ ] **Step 5 : Commit**

```bash
git add src/analytic_thacker.py tests/test_analytic_thacker.py
git commit -m "feat(v2.5): Thacker planaire diagonal (front translatant) + masque bande-front"
```

---

### Task 3 : `scripts/run_w0_representation.py` — plafond POD front-localisé (test forçant, parallèle)

**Files:** Create `scripts/run_w0_representation.py` ; `docs/v2.5_W0_nwidth.md` (écrit par le script) ; Test `tests/test_run_w0.py`

**Interfaces:**
- Consumes : `src.analytic_thacker` (`thacker_radial`, `thacker_planar_diag`, `*_period`, `front_band_mask`) ; `src.pod` (`fit_pod`, `encode`, `decode`, `stack_height`, `unstack_height`) ; `src.metrics.relative_l2_error` ; `config.GridConfig` ; matplotlib.
- Produces :
  - `band_l2_error(recon_seq, true_seq, eps=1e-3, width=2) -> float` — erreur L2 relative restreinte aux cellules de bande-front (union des bandes sur toutes les frames). C'est la métrique du **gate**.
  - `nwidth_ceiling(true_seq, H, W, energy_threshold=0.9999, max_modes=2000) -> dict` — POD hauteur sur la séquence, retourne `{k, global_err, band_err}` (global vs front-localisé).
  - `main()` — fait tourner les DEUX oracles (radial, planaire diagonal), écrit figure + verdict.

- [ ] **Step 1 : Test qui échoue**

```python
# tests/test_run_w0.py
import sys
from pathlib import Path
import numpy as np
ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))


def test_band_error_exceeds_global_on_moving_front():
    """Cœur du raffinement #1 : sur un front mobile axisymétrique, l'erreur sur la
    bande-front doit DÉPASSER l'erreur globale (le bulk lisse dilue la difficulté)."""
    from scripts.run_w0_representation import nwidth_ceiling
    from config import GridConfig
    from src.analytic_thacker import thacker_radial, thacker_radial_period
    g = GridConfig(H=64, W=64, dx=4.0 / 64, dy=4.0 / 64)
    T = thacker_radial_period()
    seq = np.stack([thacker_radial(g, t=ph * T)[0] for ph in np.linspace(0, 1, 60)])
    res = nwidth_ceiling(seq, 64, 64, energy_threshold=0.999, max_modes=64)
    assert res["k"] >= 1
    assert 0.0 <= res["global_err"]
    assert res["band_err"] > res["global_err"]    # le front est plus dur que le bulk
```

- [ ] **Step 2 : Lancer → FAIL** (module manquant).

- [ ] **Step 3 : Implémenter `scripts/run_w0_representation.py`**

```python
"""W0 — Test forçant n-width : une base POD LINÉAIRE représente-t-elle un trait d'eau
MOBILE ? Oracle analytique (Thacker, sans solveur). Signal PARALLÈLE qui dit quel réduit
aval (DMD linéaire vs encodeur) accoler à l'oracle mouillé/sec.

Raffinement clé : le GATE se lit sur l'erreur FRONT-LOCALISÉE, pas la L2 globale (le bulk
lisse axisymétrique est bas-rang et masquerait la difficulté du front). Deux oracles :
radial (front courbe qui respire = forme) et planaire diagonal (front qui translate =
transport). Les faire tourner désambiguïse forme vs transport.

Usage : .venv/bin/python scripts/run_w0_representation.py
Sorties : outputs/v2.5/w0_nwidth.png, docs/v2.5_W0_nwidth.md."""
from __future__ import annotations

import sys
from pathlib import Path
ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

import numpy as np
import matplotlib.pyplot as plt

from config import GridConfig
from src.analytic_thacker import (thacker_radial, thacker_planar_diag,
                                  thacker_radial_period, thacker_planar_period,
                                  front_band_mask)
from src.pod import fit_pod, encode, decode, stack_height, unstack_height
from src.metrics import relative_l2_error

GRID = GridConfig(H=64, W=64, dx=4.0 / 64, dy=4.0 / 64)
OUT_FIG = ROOT / "outputs" / "v2.5"
OUT_DOC = ROOT / "docs" / "v2.5_W0_nwidth.md"
N_FRAMES = 80
ENERGY, MAX_MODES = 0.9999, 2000


def band_l2_error(recon_seq, true_seq, eps: float = 1e-3, width: int = 2) -> float:
    """Erreur L2 relative restreinte aux cellules de bande-front (par frame, agrégée)."""
    num = den = 0.0
    for rec, tru in zip(recon_seq, true_seq):
        band = front_band_mask(tru, eps, width)
        if band.any():
            num += float(np.sum((rec[band] - tru[band]) ** 2))
            den += float(np.sum(tru[band] ** 2))
    return float(np.sqrt(num / (den + 1e-12)))


def nwidth_ceiling(true_seq, H: int, W: int, energy_threshold: float = ENERGY,
                   max_modes: int = MAX_MODES) -> dict:
    """POD hauteur (n_channels=1) sur la séquence ; erreur de reconstruction globale ET
    front-localisée (le gate se lit sur band_err)."""
    X = stack_height(true_seq)
    basis = fit_pod(X, energy_threshold, max_modes, n_channels=1)
    recon = unstack_height(decode(basis, encode(basis, X)), H, W)
    return {"k": basis.Phi.shape[1],
            "global_err": relative_l2_error(recon, true_seq),
            "band_err": band_l2_error(recon, true_seq)}


def _seq(kind: str):
    if kind == "radial":
        T = thacker_radial_period()
        return np.stack([thacker_radial(GRID, t=ph * T)[0]
                         for ph in np.linspace(0, 1, N_FRAMES)])
    T = thacker_planar_period()
    return np.stack([thacker_planar_diag(GRID, t=ph * T)[0]
                     for ph in np.linspace(0, 1, N_FRAMES)])


def main() -> None:
    OUT_FIG.mkdir(parents=True, exist_ok=True)
    results = {}
    for kind in ("radial", "planar_diag"):
        seq = _seq(kind)
        results[kind] = nwidth_ceiling(seq, 64, 64)
        m = results[kind]
        print(f"[W0] {kind:12s} k={m['k']:4d}  global={m['global_err']:.4f}  "
              f"FRONT={m['band_err']:.4f}")

    # figure : barres global vs front, par oracle
    fig, ax = plt.subplots(figsize=(7, 4))
    labels, glob, band = list(results), [], []
    for kk in labels:
        glob.append(results[kk]["global_err"]); band.append(results[kk]["band_err"])
    x = np.arange(len(labels)); w = 0.35
    ax.bar(x - w / 2, glob, w, label="L2 globale (trompeuse)")
    ax.bar(x + w / 2, band, w, label="L2 FRONT-localisée (gate)", color="#c33")
    ax.set_xticks(x); ax.set_xticklabels(labels); ax.set_ylabel("erreur L2 relative (h)")
    ax.set_title("W0 — plafond n-width : front mobile vs base POD linéaire")
    ax.legend(); fig.tight_layout()
    fig_path = OUT_FIG / "w0_nwidth.png"
    fig.savefig(fig_path, dpi=120); plt.close(fig)

    worst_band = max(results[k]["band_err"] for k in results)
    if worst_band > 0.20:
        verdict = (f"La base POD linéaire NE TIENT PAS le trait d'eau mobile (erreur "
                   f"front max {worst_band:.2f} > 20%). C'est le test forçant n-width "
                   f"attendu : l'approche linéaire (POD+DMD) casse en sec -> l'encodeur "
                   f"non-linéaire est JUSTIFIÉ avec preuve. Le réduit aval à accoler à "
                   f"l'oracle W1 est l'encodeur, pas DMD. (Décision gate sur le FRONT, "
                   f"pas le global — voir l'écart dans la figure.)")
    else:
        verdict = (f"La base POD linéaire tient le front mobile (erreur front max "
                   f"{worst_band:.2f} <= 20%) — résultat peu attendu mais décisif : "
                   f"DMD linéaire reste candidat sur le régime sec. Continuer W2 avec "
                   f"l'oracle W1 et le réduit linéaire.")

    OUT_DOC.parent.mkdir(parents=True, exist_ok=True)
    lines = ["# W0 — Test forçant n-width (Thacker, sans solveur)", "",
             "POD hauteur (n_channels=1) sur un trait d'eau MOBILE analytique. **Gate sur "
             "l'erreur FRONT-localisée** (le bulk lisse dilue la L2 globale).", "",
             "| oracle | k | L2 globale | L2 FRONT (gate) |", "|---|---|---|---|"]
    for kk in results:
        m = results[kk]
        lines.append(f"| {kk} | {m['k']} | {m['global_err']:.4f} | **{m['band_err']:.4f}** |")
    lines += ["", "Radial = front courbe qui *respire* (forme) ; planar_diag = front qui "
              "*translate* (transport, tueur n-width). L'écart global vs front mesure le "
              "masquage de régime évité.", "", "## Verdict", "", verdict, "",
              f"Figure : `{fig_path.relative_to(ROOT)}`."]
    OUT_DOC.write_text("\n".join(lines) + "\n")
    print(f"[W0] note -> {OUT_DOC}")
    print(f"[W0] VERDICT : {verdict}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4 : Lancer le test → PASS.** `.venv/bin/python -m pytest tests/test_run_w0.py -q`

- [ ] **Step 5 : Lancer W0 (le signal parallèle)** : `.venv/bin/python scripts/run_w0_representation.py`. Attendu : pour chaque oracle, `k`, L2 globale, L2 FRONT. **Rapporter les deux ; lire le verdict sur le FRONT.** Attendu (probable) : erreur front élevée (≫ global) → n-width confirmée. Ne pas maquiller.

- [ ] **Step 6 : Commit**

```bash
git add scripts/run_w0_representation.py tests/test_run_w0.py docs/v2.5_W0_nwidth.md
git commit -m "feat(v2.5): W0 — plafond n-width front-localisé (radial+diagonal), verdict"
```

---

### Task 4 : `src/ritter.py` — solution analytique de Ritter (référence de validation W1)

**Files:** Create `src/ritter.py` ; Test `tests/test_ritter.py`

**Interfaces:**
- Consumes : `config.GRAVITY`.
- Produces : `ritter_dam_break_dry(x, t, hl=0.005, x0=5.0) -> np.ndarray` → hauteur `h(x,t)` 1D (vecteur même forme que `x`), dam-break sur lit sec (SWASHES §4.1.2, vérifié).

- [ ] **Step 1 : Test qui échoue**

```python
# tests/test_ritter.py
import numpy as np
import pytest
from config import GRAVITY
from src.ritter import ritter_dam_break_dry


def test_ritter_front_position_and_profile():
    hl, x0, t = 0.005, 5.0, 6.0
    x = np.linspace(0, 10, 1001)
    h = ritter_dam_break_dry(x, t, hl, x0)
    c = np.sqrt(GRAVITY * hl)
    xA, xB = x0 - t * c, x0 + 2 * t * c           # bords amont/aval de la raréfaction
    assert (h[x <= xA] == pytest.approx(hl)).all()       # plateau amont intact
    assert (h[x >= xB] == 0.0).all()                     # sec en aval du front
    assert (h >= 0).all()
    # à la position du barrage, h = (4/9) hl (valeur de Ritter)
    i0 = np.argmin(np.abs(x - x0))
    assert h[i0] == pytest.approx(4.0 / 9.0 * hl, rel=1e-2)
```

- [ ] **Step 2 : Lancer → FAIL.**

- [ ] **Step 3 : Implémenter (verbatim — SWASHES §4.1.2)**

```python
"""Solution analytique de Ritter : rupture de barrage sur lit SEC, sans friction.
Vérifiée dans SWASHES §4.1.2 (arXiv:1110.0288). Référence de validation du solveur
mouillé/sec (position et forme du front). h_l à gauche de x0, sec (0) à droite, u=0 à t=0."""
from __future__ import annotations

import numpy as np

from config import GRAVITY


def ritter_dam_break_dry(x: np.ndarray, t: float, hl: float = 0.005,
                         x0: float = 5.0) -> np.ndarray:
    """h(x,t) pour t>0. Trois zones : plateau (h_l), raréfaction (parabole), sec (0)."""
    x = np.asarray(x, dtype=np.float64)
    c = np.sqrt(GRAVITY * hl)
    if t <= 0:
        return np.where(x <= x0, hl, 0.0)
    xA = x0 - t * c
    xB = x0 + 2.0 * t * c
    h = np.empty_like(x)
    h[:] = 0.0
    h[x <= xA] = hl
    mid = (x > xA) & (x < xB)
    h[mid] = (4.0 / (9.0 * GRAVITY)) * (np.sqrt(GRAVITY * hl) - (x[mid] - x0) / (2.0 * t)) ** 2
    return h
```

- [ ] **Step 4 : Lancer → PASS.**
- [ ] **Step 5 : Commit** `git add src/ritter.py tests/test_ritter.py && git commit -m "feat(v2.5): solution analytique Ritter (dam-break lit sec, réf. validation W1)"`

---

### Task 5 : `src/solver_wetdry.py` — solveur mouillé/sec (hydrostatic reconstruction + HLL + SSP-RK2)

**Files:** Create `src/solver_wetdry.py` ; Test `tests/test_solver_wetdry.py`

**Interfaces:**
- Consumes : `config.GRAVITY`, `config.GridConfig`, `config.SolverConfig`.
- Produces :
  - `simulate_wetdry(h0, hu0, hv0, b, grid, cfl=0.4, t_end=..., dry_eps=1e-4) -> tuple[list[float], np.ndarray, np.ndarray, np.ndarray]` → `(times, h_seq, hu_seq, hv_seq)`, séquences `(T,H,W)`. Variables conservatives en interne.
  - `desingularize_velocity(h, hu, dry_eps) -> u` (helper).

**Recette (Audusse et al. 2004 + HLL ; recouper le terme source contre la référence — la C-property l'attrape) :**
- Variables conservatives `q=(h, hu, hv)` ; vitesses par désingularisation (`u=hu/h` plafonné, `u=0` si `h≤dry_eps`).
- Reconstruction hydrostatique à chaque interface : `z* = max(z_L, z_R)`, `h*_L = max(0, (h_L+z_L) − z*)`, `h*_R = max(0, (h_R+z_R) − z*)` ; états reconstruits `(h*_K, h*_K·u_K)`.
- Flux **HLL** sur les états reconstruits ; vitesses d'onde Davis `s_L=min(u_L−c_L, u_R−c_R)`, `s_R=max(u_L+c_L, u_R+c_R)`, `c=√(g h*)` ; **dry-aware** : côté sec → vitesse fixée par la raréfaction du côté mouillé.
- **Correction well-balanced** : au flux d'interface, ajouter aux termes de quantité de mouvement la correction de pression `(g/2)(h_K² − h*_K²)` côté `K` (Audusse). *C'est le terme à recouper contre Audusse 2004 ; la C-property (Task 6) le valide.*
- **SSP-RK2** : `U1 = U + Δt L(U)` ; `U^{n+1} = ½U + ½(U1 + Δt L(U1))`.
- **Pas de temps positivity-preserving** : `Δt = cfl · min(dx,dy) / max(|u|+c, |v|+c)`, avec `cfl ≤ 0.4` ; cellules `h≤dry_eps` mises à `h=0, hu=hv=0` après chaque étage.

- [ ] **Step 1 : Tests qui échouent (les invariants — la masse ne suffit pas)**

```python
# tests/test_solver_wetdry.py
import numpy as np
import pytest
from config import GridConfig, GRAVITY
from src.solver_wetdry import simulate_wetdry, desingularize_velocity

GRID = GridConfig(H=32, W=32, dx=4.0 / 32, dy=4.0 / 32)


def test_desingularize_zero_below_dry():
    h = np.array([0.0, 1e-6, 1.0]); hu = np.array([0.1, 0.1, 2.0])
    u = desingularize_velocity(h, hu, dry_eps=1e-4)
    assert u[0] == 0.0 and u[1] == 0.0            # sec -> vitesse nulle (pas d'explosion)
    assert u[2] == pytest.approx(2.0)


def test_c_property_lake_at_rest_emergent_bathy():
    """C-PROPERTY : lac au repos (η=const) sur bathymétrie ÉMERGENTE (île qui perce la
    surface) reste au repos -> vitesse parasite ~ 0. C'est le test du well-balancing ET
    le garde-fou qui attrape une erreur dans le terme source d'Audusse."""
    yy, xx = np.mgrid[0:32, 0:32].astype(float)
    b = 0.6 * np.exp(-(((xx - 16) ** 2 + (yy - 16) ** 2) / 18.0))  # île, sommet 0.6
    eta0 = 0.4                                     # surface < sommet -> île émergée
    h0 = np.maximum(eta0 - b, 0.0)                 # sec sur l'île, mouillé autour
    z = np.zeros_like(h0)
    times, hs, hus, hvs = simulate_wetdry(h0, z.copy(), z.copy(), b, GRID,
                                          cfl=0.4, t_end=2.0, dry_eps=1e-4)
    speed = np.sqrt((hus / np.maximum(hs, 1e-6)) ** 2
                    + (hvs / np.maximum(hs, 1e-6)) ** 2)
    assert float(speed.max()) < 1e-6              # repos préservé (well-balanced)
    assert float(hs.min()) >= 0.0                 # positivité


def test_positivity_on_drying():
    """Une goutte qui s'étale sur un fond sec ne doit JAMAIS produire h<0."""
    yy, xx = np.mgrid[0:32, 0:32].astype(float)
    b = np.zeros((32, 32))
    h0 = np.maximum(0.5 * np.exp(-(((xx - 16) ** 2 + (yy - 16) ** 2) / 4.0)) - 0.05, 0.0)
    z = np.zeros_like(h0)
    times, hs, hus, hvs = simulate_wetdry(h0, z.copy(), z.copy(), b, GRID,
                                          cfl=0.4, t_end=1.0, dry_eps=1e-4)
    assert float(hs.min()) >= 0.0                 # positivité tout le temps
    assert np.isfinite(hs).all()                  # pas de NaN/Inf (désingularisation OK)
```

- [ ] **Step 2 : Lancer → FAIL** (module manquant).

- [ ] **Step 3 : Implémenter `src/solver_wetdry.py`**

> Implémenter la recette ci-dessus. Squelette complet (flux HLL + reconstruction
> hydrostatique + RK2) — l'implémenteur écrit le corps en suivant la recette, **recoupe
> le terme source contre Audusse 2004**, et fait passer les invariants. Structure
> conseillée (miroir de `solver.py` : flux x puis y, parois réfléchissantes en bord de
> domaine via padding `edge` sur η et négation de la quantité de mouvement normale) :

```python
"""W1 — Solveur shallow-water MOUILLÉ/SEC (well-balanced + positivity-preserving).

Reconstruction hydrostatique (Audusse et al. 2004) + flux HLL + gestion mouillé/sec +
SSP-RK2 (1er ordre). NOUVEAU schéma — n'altère PAS src/solver.py (Rusanov submergé).
Validé contre solutions analytiques (cf. scripts/run_w1_validate.py), pas par la seule
masse. Variables conservatives q=(h, hu, hv). Indexation array[y, x]."""
from __future__ import annotations

import numpy as np
from config import GRAVITY, GridConfig


def desingularize_velocity(h, hu, dry_eps: float):
    """u = hu/h, mais 0 si h<=dry_eps (jamais de division explosive près du sec)."""
    u = np.zeros_like(h)
    wet = h > dry_eps
    u[wet] = hu[wet] / h[wet]
    return u


def _hll_flux(hL, huL, hR, huR, dry_eps):
    """Flux HLL 1D (composantes masse + qdm normale) sur états déjà reconstruits.
    Vitesses d'onde Davis, dry-aware. Retourne (F_h, F_hu)."""
    g = GRAVITY
    uL = desingularize_velocity(hL, huL, dry_eps)
    uR = desingularize_velocity(hR, huR, dry_eps)
    cL, cR = np.sqrt(g * np.maximum(hL, 0.0)), np.sqrt(g * np.maximum(hR, 0.0))
    # vitesses d'onde dry-aware : côté sec -> raréfaction du côté mouillé
    sL = np.where(hL > dry_eps, np.minimum(uL - cL, uR - cR), uR - 2.0 * cR)
    sR = np.where(hR > dry_eps, np.maximum(uL + cL, uR + cR), uL + 2.0 * cL)
    # flux physiques
    FhL, FhR = huL, huR
    FhuL = huL * uL + 0.5 * g * hL ** 2
    FhuR = huR * uR + 0.5 * g * hR ** 2
    def hll(FL, FR, UL, UR):
        out = np.where(sL >= 0, FL,
              np.where(sR <= 0, FR,
              (sR * FL - sL * FR + sL * sR * (UR - UL)) / (sR - sL + 1e-300)))
        return out
    return hll(FhL, FhR, hL, hR), hll(FhuL, FhuR, huL, huR)
# ... reconstruction hydrostatique aux interfaces x et y, correction de pression
#     (g/2)(h_K^2 - h*_K^2) côté K, assemblage L(U), SSP-RK2, plancher sec, CFL
#     positivity-preserving. Voir recette ci-dessus + Audusse 2004 (C-property = gate).


def simulate_wetdry(h0, hu0, hv0, b, grid, cfl=0.4, t_end=2.0, dry_eps=1e-4):
    ...  # boucle RK2 jusqu'à t_end, renvoie (times, h_seq, hu_seq, hv_seq)
```

> Note d'implémentation : commencer par faire passer `test_c_property_lake_at_rest_emergent_bathy` (il valide la reconstruction hydrostatique + le terme source) AVANT les autres. Si la vitesse parasite n'est pas ~0, le terme source est mal placé → recouper Audusse 2004.

- [ ] **Step 4 : Lancer → PASS** (3 invariants : désingularisation, C-property, positivité).
- [ ] **Step 5 : Commit** `git add src/solver_wetdry.py tests/test_solver_wetdry.py && git commit -m "feat(v2.5): solveur mouillé/sec (hydrostatic recon + HLL + RK2), invariants C-property/positivité"`

---

### Task 6 : `scripts/run_w1_validate.py` — les 4 validations analytiques (porte d'acceptation de l'oracle)

**Files:** Create `scripts/run_w1_validate.py` ; `docs/v2.5_W1_validation.md` (écrit par le script)

**Interfaces:**
- Consumes : `src.solver_wetdry.simulate_wetdry` ; `src.analytic_thacker` (radial : champ + bathy + période) ; `src.ritter.ritter_dam_break_dry` ; `src.metrics` ; `config` ; matplotlib.
- Produces : `main()` qui exécute les 4 validations, écrit figures + doc, et **assert chaque critère** (le script échoue si l'oracle n'est pas valide).

**Les 4 validations (mass nécessaire mais pas suffisant) :**
1. **Thacker radial** : initialiser avec `thacker_radial(t=0)`, simuler 1 période, comparer `h` à l'analytique à plusieurs instants → erreur L2 **bornée dans le temps** (< ~5–10 %, croissance non monotone).
2. **Ritter (lit sec, 1D-en-2D)** : marche `h_l/0`, comparer à `ritter_dam_break_dry` à `t=6` → position du front correcte (à ±2 cellules), pas de `h<0`, pas de point parasite au passage sec→mouillé.
3. **C-property** : lac au repos sur île émergente → vitesse parasite max `< 1e-6` (déjà en test unitaire Task 5 ; ici sur la grille 64×64 et reporté).
4. **Positivité** : `min(h) ≥ 0` sur toutes les frames des cas 1–3.

- [ ] **Step 1 : Implémenter `scripts/run_w1_validate.py`** (assemble les 4 ; chaque critère est un `assert`, et un tableau récapitulatif + figures vérité|simulé|résidu pour Thacker et Ritter ; écrit `docs/v2.5_W1_validation.md`).
- [ ] **Step 2 : Lancer** : `.venv/bin/python scripts/run_w1_validate.py`. **Porte d'acceptation** : les 4 passent → l'oracle W1 est digne de confiance, W2 peut être planifié. Un échec = le solveur n'est pas un oracle valide ; corriger (typiquement le terme source pour la C-property, ou les vitesses d'onde dry-aware pour Ritter) — **ne pas** desserrer le critère.
- [ ] **Step 3 : Vérifier la non-régression** : `.venv/bin/python -m pytest -q` (les 55 tests POC/v2 + les nouveaux v2.5 passent ; `solver.py` intact).
- [ ] **Step 4 : Commit** `git add scripts/run_w1_validate.py docs/v2.5_W1_validation.md && git commit -m "feat(v2.5): W1 — 4 validations analytiques (Thacker/Ritter/C-property/positivité), porte oracle"`

---

## W2 / W3 — conditionnels (non détaillés)

- **W2** (après l'oracle W1 validé) : avec `simulate_wetdry`, générer la vraie dynamique mouillé/sec (îles émergentes, dam-break, sillages), POD-reconstruire, mesurer le plafond **front-localisé** (analogue de V1/W0, régime dur), interp vs extrap. Gate : plafond front élevé → encodeur justifié avec test forçant *réaliste* ; acceptable → W3.
- **W3** (après le chiffre de W2) : dynamique + **projection masse-conservante ET positivity-preserving** (redistribution sur les cellules mouillées — la projection M5 uniforme du POC ne marche plus en sec).

---

## Self-Review

**Couverture spec** : W0 test forçant analytique + gate front-localisé (Tasks 1–3, raffinement #1) ✓ ; deux oracles forme/transport (Tasks 1–2, raffinement #2) ✓ ; W1 hydrostatic reconstruction + HLL + gestion sec + SSP-RK2 (Task 5) ✓ ; 4 validations analytiques (Tasks 4,6) ✓ ; ne pas modifier solver.py / non-régression (contraintes + Task 6 step 3) ✓ ; W2/W3 conditionnels (discipline diagnostic-first) ✓. W0 ∥ W1 indépendants (fichiers disjoints) ✓.

**Placeholders** : W0 et les références analytiques (Thacker, Ritter) ont du code complet vérifié. Le **corps** du solveur W1 (Task 5 step 3) est donné en recette + squelette + invariants-gate plutôt qu'en 200 lignes potentiellement fausses : c'est délibéré et signalé (le terme source d'Audusse est l'erreur probable ; la C-property l'attrape). C'est le seul endroit non-100%-verbatim, justifié par le fait que la **validation analytique** est le mécanisme de correction (cf. discipline « l'oracle ne vaut que ses validations »).

**Cohérence des types** : `thacker_radial`/`thacker_planar_diag` → `(h,b)` consommés par W0 (h) et W1 (h,b) ; `front_band_mask` consommé par `band_l2_error` ; `simulate_wetdry(h0,hu0,hv0,b,grid,...)` → `(times,h_seq,hu_seq,hv_seq)` consommé par les validations ; `n_channels=1` partout pour la POD hauteur.

---

## Execution Handoff

Plan sauvegardé. Exécution recommandée : **subagent-driven-development**. W0 (Tasks 1–3) et W1 (Tasks 4–6) sont **indépendants** → peuvent être menés en parallèle par des subagents distincts (fichiers disjoints), W0 étant le signal gratuit. W2/W3 planifiés après les chiffres de W1 (validations) puis W2.
