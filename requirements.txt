# Cœur (CPU) — POC fluide réduit
numpy>=1.26
scipy>=1.11
matplotlib>=3.8

# Arc C Task 3 — validation du contrat de sortie pins_spatial.json (§C10,
# schemas/arcC-pins-spatial-v1.schema.json) ; pur Python, sans C, n'affecte
# aucune autre dépendance.
jsonschema>=4.0

# Arc C session HUMAINE uniquement — backend GUI matplotlib (fenêtre + capture
# clavier a/b). Sans lui, matplotlib se rabat sur Agg (non-interactif) et la
# session boucle sans fin (cf. src/arcC_backend.py, garde fail-loud). NON requis
# pour les tests (Agg suffit). Qt auto-contenu, n'affecte pas la stack JAX/warp.
PySide6>=6.0

# Tests (dev uniquement)
pytest>=8.0

# Optionnel : export GIF des animations. Sans pillow, fallback montage PNG (matplotlib seul).
# pillow>=10.0

# M4/M5 uniquement (déféré, GPU CUDA RTX 3050 Ti <=4 Go, AMP). NE PAS installer avant M4.
# torch>=2.2
